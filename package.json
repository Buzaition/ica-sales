{
  "name": "workspace",
  "version": "0.0.0",
  "packageManager": "pnpm@9.15.9",
  "license": "MIT",
  "scripts": {
    "dev:client": "corepack pnpm --filter @workspace/client dev",
    "dev:server": "corepack pnpm --filter @workspace/server dev",
    "build": "corepack pnpm run typecheck && corepack pnpm -r --if-present run build",
    "typecheck": "corepack pnpm -r --if-present run typecheck && tsc -p tsconfig.api.json --noEmit"
  },
  "private": true,
  "devDependencies": {
    "@types/node": "catalog:",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
